module mini-sdf/sorts/sorts

imports
  signatures/mini-sdf/mini-sdf-sig
  cons-type-interface/types
  cons-type-interface/sorts

rules

  sortsOk maps sortOk(*, list(*))

  sortOk: scope * ID
  sortOk(s, ID-LEX2ID(n)) :-
    declareSort(s, n).
